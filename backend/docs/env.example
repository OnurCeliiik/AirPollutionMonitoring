# API Configuration
PORT=8080
ENVIRONMENT=development # development or production
LOG_LEVEL=INFO # DEBUG, INFO, WARN, ERROR, FATAL

# Security
ALLOWED_ORIGINS=* # Comma-separated domains in production, e.g., https://example.com,https://dashboard.example.com

# Kafka Configuration
KAFKA_BROKERS=kafka:29092

# Database Configuration
DB_CONNECTION_STRING=postgres://postgres:postgres@timescaledb:5432/timescaledb?sslmode=disable

# Secrets (use Docker secrets in production)
DB_PASSWORD=postgres

# Ingest Service Only
# No service-specific variables

# Processor Service Only
# No service-specific variables

# Notifier Service Only
NOTIFIER_PORT=8081 